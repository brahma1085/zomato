server:
  port: 8083

spring:
  application:
    name: search-service
  elasticsearch:
    uris: ${ELASTICSEARCH_URL:http://localhost:9200}
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
